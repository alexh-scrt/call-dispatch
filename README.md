# call-dispatch
call_dispatch is a lightweight, locally-runnable AI agent phone call dispatcher that enables automated outbound calls for simple tasks like appointment booking, quote requests, and availability checks. It integrates with Twilio for telephony, uses OpenAI's GPT models for conversation logic and summary generation, and Deepgram for real-time transcri
